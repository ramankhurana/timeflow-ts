# timeflot-ts

`timeflot-ts` is a one-stop library for time series experimentation: loading, sanity checking, preprocessing (missing values, sentinel defaults, outliers), plotting, metrics, benchmarking, and consistent train/val/test scaling.

